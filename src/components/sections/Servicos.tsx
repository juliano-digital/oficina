import { Cog, Droplets, ScanLine, ShieldCheck, Wrench } from 'lucide-react'
import { motion } from 'framer-motion'
import { useServices } from '../../hooks/useServices'
import { SectionHeading } from '../ui/SectionHeading'
import type { Service } from '../../types/service'
const iconMap = { Cog, Droplets, ScanLine, ShieldCheck, Wrench }
const fallback: Service[] = [{ id: '1', titulo: 'Diagnóstico eletrônico', descricao: 'Leitura completa dos módulos para encontrar a causa real do defeito.', icone: 'ScanLine', ordem: 1 }, { id: '2', titulo: 'Troca de óleo ATF', descricao: 'Fluido correto, procedimento técnico e filtro novo para proteger seu câmbio.', icone: 'Droplets', ordem: 2 }, { id: '3', titulo: 'Reparo de câmbio', descricao: 'Desmontagem, inspeção e montagem com peças selecionadas e garantia.', icone: 'Cog', ordem: 3 }, { id: '4', titulo: 'Manutenção preventiva', descricao: 'Revisões programadas para evitar trancos, patinação e prejuízo.', icone: 'ShieldCheck', ordem: 4 }]
export function Servicos() { const { services, loading } = useServices(); const list = services.length ? services : fallback; return <section id="servicos" className="bg-surface px-5 py-24 lg:px-8"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow="O que fazemos" title={<>Serviços para o seu câmbio <span className="text-signal">render mais.</span></>} description={loading ? 'Carregando nosso catálogo...' : 'Tudo o que seu automático precisa, no mesmo lugar.'} /><div className="mt-14 grid gap-4 md:grid-cols-2">{list.map((service, index) => { const Icon = iconMap[service.icone as keyof typeof iconMap] ?? Wrench; return <motion.article whileHover={{ backgroundColor: '#191919' }} key={service.id} className="group border border-line p-6 transition-colors"><div className="flex items-start justify-between"><div className="flex h-12 w-12 items-center justify-center bg-signal text-white"><Icon size={22} /></div><span className="font-display text-5xl text-white/10">0{index + 1}</span></div><h3 className="mt-10 font-display text-3xl uppercase text-white">{service.titulo}</h3><p className="mt-3 max-w-md text-sm leading-6 text-mist">{service.descricao}</p></motion.article> })}</div></div></section> }
